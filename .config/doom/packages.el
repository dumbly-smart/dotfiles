;; -*- no-byte-compile: t; -*-
;;; packages.el

;; Doom's enabled modules currently provide everything in this setup.
;; Add third-party packages here with (package! package-name), then run:
;;   doom sync
